	整个思路应该没有太大的问题，但由于在刚开始使用RSA进行加密和解密时，使用函数Math.pow(encryptResult, ned[1]) % ned[0];其中encrytResult和ned[1]值过大，没有办法很好的进行加密和解密，而后尝试使用快速模幂运算，该问题得到了部分解决。但题目要求是取大数x，为了方便限制取的大数x值都比较小，所以适当大小的素数 p并不是那么的好取，也可能会出现k-j<0（k为x加密后的数）的情况，需要再次运行直到符合要求。