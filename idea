百万富翁问题（B为李，A为王）
思路：
第一步：B无私钥，有公钥
1、B选出大数x，加密E(x)=k（D(k)=x，但B无法解密）
2、计算k-j+1=m，B将m给A（A不知道k，所以无法计算j）
第二步：A有公钥，有私钥
1、计算k-j+1,k-j+2 ... k-j+j,k-j+10（包含k的情况）
2、解密yu=D(k-j+u)（其中yj=D(k)，即为x)
3、求模zu=yu mod p（p为质数）
4、z1到zi不变，之后的全部加1，传给b
第三步：B
如果x mod p=zj，则j<=i，反之j>i